# Ralph Progress Log
Started: 2026-02-24

## Codebase Patterns
- Use Node built-in test runner (`node --test`) for tests; test files use `.test.mjs` extension.
- Shared math fixtures live in `tests/fixtures/math-fixtures.js`; tolerance helpers in `tests/helpers/tolerance.js`.
- Project uses `"type":"module"` in package.json; release.cjs is CommonJS for build script compatibility.
- Pure coordinate math (cartesianToIso, isoToCartesian) in utils.js has no Foundry deps and can be tested in Node.
- Extract placement/transform math to pure functions in utils.js for unit testing; transform.js calls them with document values.
- Use safeDivide(num, denom, fallback) for any division where denominator can be 0 or non-finite.
- computeOffsetComponentsForProjection is the shared path for offset/elevation; transform and ruler both use it.
- Depth sorting uses visual center (mesh.position); computeVisualYForSort projects grid position to Visual Y for sort.
- Coordinate conversion helpers (cartesianToIsoProjection, isoToCartesianProjection) accept projection object with reverseRotation; use ISOMETRIC_CONST for default 45° projection.

---
## 2026-02-24 - US-001
- Implemented shared math fixtures (projection names, token dimensions, grid sizes/distances, offsets, elevations) in tests/fixtures/math-fixtures.js.
- Added tolerance helper (assertAlmostEqual, assertPointAlmostEqual) in tests/helpers/tolerance.js.
- Created tests/utils.test.mjs consuming fixtures and helper to test cartesianToIso, isoToCartesian, calculateIsometricVerticalDistance.
- Added npm test script using Node --test; added "type":"module" and release.cjs for ESM/CommonJS coexistence.
- Files changed: tests/fixtures/math-fixtures.js (new), tests/helpers/tolerance.js (new), tests/utils.test.mjs (new), package.json, release.cjs (new), eslint.config.cjs, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Scripts use ES modules; release.js was CommonJS—migrated to release.cjs to allow "type":"module" for tests.
  - ESLint has a release.cjs override with globals.node; tests/** override uses globals.node for Node test files.
  - Fixtures match the Math Test Matrix dimensions from the PRD (projection, token shape, grid, offsets, elevation).
---
## 2026-02-24 - US-002
- Fixed axis-scale token placement: center and projected offsets now use scaleX for X and scaleY for Y (was incorrectly using scaleX for both).
- Extracted computeTokenPlacementPosition(docX, docY, scaleX, scaleY, gridSize, isoOffsets) to utils.js for testability.
- Updated transform.js to use the shared helper.
- Added 6 unit tests for computeTokenPlacementPosition: square 1x1 baseline, rectangular 2x1, 1x2, 3x2, projected offset scaling, and fixture coverage.
- Files changed: scripts/utils.js, scripts/transform.js, tests/utils.test.mjs, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Token placement formula: pos.x = docX + (scaleX * gridSize/2) + (scaleX * isoOffsets.x); pos.y = docY + (scaleY * gridSize/2) + (scaleY * isoOffsets.y).
  - Extracting pure placement math to utils enables Node tests without Foundry mocks.
  - Manual browser verification needed: place 2x1, 1x2, 3x2 tokens in Foundry isometric scene to confirm no drift.
---
## 2026-02-24 - US-003
- Added safeDivide(num, denom, fallback) in utils.js: returns fallback when denominator is 0/NaN or numerator non-finite.
- Added computeElevationOffsetDelta and computeElevationVisualOffset with guards for gridDistance=0, scaleX=0, non-finite elevation.
- Guarded transform.js: elevation math, objTxtRatio divisions, tile scale (originalWidth/Height), resetTokenTransform, resetTileTransform, updateTokenVisuals shadow/line.
- Guarded ruler.js: gridSize/gridDist factor, elev*factor/(doc.width), zoom division.
- Added 10 unit tests for safeDivide, computeElevationOffsetDelta, computeElevationVisualOffset (grid distance 0, scaleX 0, non-finite inputs).
- Files changed: scripts/utils.js, scripts/transform.js, scripts/ruler.js, tests/utils.test.mjs, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Use safeDivide for any division where denominator can be 0 or non-finite (gridDistance, gridSize, scaleX, originalWidth/Height, objTxtRatio).
  - computeElevationOffsetDelta and computeElevationVisualOffset centralize elevation math with guards; transform and ruler both use them.
---
## 2026-02-24 - US-004
- Added computeOffsetComponentsForProjection(artOffsetX, artOffsetY, elevation, gridSize, gridDistance, scaleX) in utils.js as shared helper.
- Transform and ruler now both use this helper for offset/elevation projection; equivalent inputs produce matching projected coordinates.
- Added 2 integration tests: transform/ruler equivalence (matching coords for token shapes × offsets), finite outputs for matrix (token × offset × elevation × grid).
- Files changed: scripts/utils.js, scripts/transform.js, scripts/ruler.js, tests/utils.test.mjs, prd.json, progress.txt.
- **Learnings for future iterations:**
  - computeOffsetComponentsForProjection is the single source of truth for offset+elevation in grid space; transform and ruler must both use it.
  - Ruler previously omitted gridSize/100 scaling for art offsets; unification fixes label drift for non-100 grid sizes.
  - Manual browser verification: measure with Token Ruler in isometric scene; labels should align with token visuals.
---
## 2026-02-24 - US-005
- Sorting now uses visual center (token.mesh.position when available) instead of doc top-left; fallback to computed center when mesh absent.
- Extracted computeVisualYForSort(x, y, rotation, skewX, skewY) as pure projection for testing.
- Added 4 integration tests: finite values for token positions, south>north ordering, deterministic overlap, scaled/offset stability.
- Files changed: scripts/utils.js, tests/utils.test.mjs, prd.json, progress.txt.
- **Learnings for future iterations:**
  - calculateTokenSortValue uses mesh.position (visual center) so scaled/offset tokens sort by rendered position, not doc corner.
  - computeVisualYForSort is the pure projection; use TRUE_ISOMETRIC_RAD from fixtures for tests.
  - Manual browser verification: overlap tokens in isometric scene; front/back order should match visuals.
---
## 2026-02-24 - US-006
- Implemented projection-aware conversion helpers: cartesianToIsoProjection(x, y, projection) and isoToCartesianProjection(isoX, isoY, projection) using projection.reverseRotation (radians).
- cartesianToIso and isoToCartesian now delegate to these with default 45° projection.
- Updated callers: transform.js, ruler.js, token.js use cartesianToIsoProjection(..., ISOMETRIC_CONST) for token/tile offsets and anchor deltas.
- Added PROJECTION_PRESETS_RAD to math-fixtures.js (True Isometric, Dimetric, Overhead, Projection 3:2, Custom).
- Added 3 unit tests: forward/inverse round-trip for all presets, custom vs default difference, custom (reverseRotation 0) output.
- Files changed: scripts/utils.js, scripts/transform.js, scripts/ruler.js, scripts/token.js, tests/fixtures/math-fixtures.js, tests/utils.test.mjs, prd.json, progress.txt.
- **Learnings for future iterations:**
  - Use cartesianToIsoProjection/isoToCartesianProjection when projection may vary; pass ISOMETRIC_CONST for default.
  - PROJECTION_PRESETS_RAD provides test fixtures for all projection types including custom.
---
## 2026-02-24 - US-007
- Created tests/matrix.test.mjs with automated matrix core combinations.
- Matrix dimensions: projection × token shape × grid config × offsets × elevation.
- Assertions: finite outputs (all combos), transform/ruler equivalence (proj × token × offset), sorting stability (token × offset + overlap), custom projection effect (non-zero offsets).
- Failure messages use label() to identify exact failing dimension combination (proj, token, gs, gd, off, elev).
- Files changed: tests/matrix.test.mjs (new), prd.json, progress.txt.
- **Learnings for future iterations:**
  - Origin (0,0) maps to (0,0) for all projections; custom vs standard differs only for non-zero offsets.
  - Matrix test runs 5×4×3×2×4×4 = 1920 finite-output combos; keep assertions focused to avoid slow runs.
---
