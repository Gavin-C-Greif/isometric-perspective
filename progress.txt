# Ralph Progress Log
Started: 2026-02-24

## Codebase Patterns
- Module targets v13 only (module.json minimum 13). Legacy v11 branches are marked "LEGACY: v11-only; not executed on v13" and are dead code on v13 runtime.
- Use workflow job name `Lint and Test` as the required branch-protection status check for `main`.
- Use `npm run harness:high-risk -- --init --force` to reset `high-risk-harness.json`, then `npm run harness:high-risk` to produce scenario-level PASS/FAIL evidence.
- Normalize occlusion setting values through `scripts/occlusion-mode.js` before runtime use so invalid `cpuN` values safely fall back to `cpu6`.
- Use `npm run harness:occlusion-baseline -- --init --force` to regenerate `occlusion-baseline-run.json`, then `npm run harness:occlusion-baseline` to verify release baseline evidence completeness.
- On occlusion teardown, remove any stage children named `debug-*` to avoid stale debug overlays across scene transitions.
- Use optional catch binding (`catch {}`) for intentional no-op teardown guards to avoid extra unused-error lint warnings.
- Dynamic tile overlay policy for US-004: render overlays only when a token is actively controlled; keep layers empty when no token is controlled.
- For linked-wall dynamic tiles, refresh overlay on both `updateWall` (door/geometry/flag changes) and `deleteWall` events when the wall id is linked by any tile.

---

## 2026-02-24 19:28:50 CST - US-001
- Implemented GitHub Actions CI quality gates for pull requests and pushes to `main`, running `npm run lint` and `npm test`.
- Files changed: `.github/workflows/ci-quality-gates.yml`, `README.md`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: Branch protection should enforce the `Lint and Test` check from the `CI Quality Gates` workflow.
  - Gotchas encountered: `npm run lint` currently reports existing warnings but exits successfully; only errors fail the gate.
  - Useful context: v13 release documentation is split across `README.md`, `RELEASE-PROCESS.md`, and `SMOKE-TEST.md`.
---

## 2026-02-24 19:31:37 CST - US-002
- Implemented a repeatable high-risk integration harness (`scripts/high-risk-harness.js`) with scenario-level pass/fail evidence output, added a default evidence template (`high-risk-harness.json`), added harness unit tests, and linked harness usage from `SMOKE-TEST.md` and `RELEASE-PROCESS.md`.
- Files changed: `package.json`, `scripts/high-risk-harness.js`, `tests/high-risk-harness.test.mjs`, `high-risk-harness.json`, `SMOKE-TEST.md`, `RELEASE-PROCESS.md`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: Keep high-risk release evidence machine-readable in `high-risk-harness.json` and validate with `npm run harness:high-risk`.
  - Gotchas encountered: Harness exits non-zero until all five scenarios are `PASS` with non-empty evidence.
  - Useful context: Browser verification for this run is blocked in this environment because browser MCP tool descriptor files were unavailable and no Foundry runtime target URL/session was provided.
---

## 2026-02-24 19:33:48 CST - US-003
- Hardened occlusion mode handling with `scripts/occlusion-mode.js` (normalized `off/gpu/cpuN` parsing and `cpu6` fallback for invalid CPU chunk values), wired it into `scripts/occlusion.js`, added mode-policy tests, and documented production mode behavior/default recommendations in `README.md` and `PERFORMANCE-BASELINE.md`.
- Files changed: `scripts/occlusion.js`, `scripts/occlusion-mode.js`, `tests/occlusion-mode.test.mjs`, `README.md`, `PERFORMANCE-BASELINE.md`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: Keep occlusion mode parsing logic isolated and testable outside Foundry/PIXI runtime dependencies.
  - Gotchas encountered: Browser MCP schema files are not present in this environment, so in-browser validation cannot be automated from this run.
  - Useful context: `US-003` still needs one manually executed release-candidate profiling baseline record plus browser verification evidence before it can be marked complete.
---

## 2026-02-24 19:35:58 CST - US-003
- Added a structured occlusion profiling evidence harness (`scripts/occlusion-baseline-harness.js`) plus template artifact (`occlusion-baseline-run.json`), tests, and release docs wiring so release-candidate baseline runs can be validated with explicit PASS/FAIL output.
- Files changed: `package.json`, `scripts/occlusion-baseline-harness.js`, `tests/occlusion-baseline-harness.test.mjs`, `occlusion-baseline-run.json`, `PERFORMANCE-BASELINE.md`, `RELEASE-PROCESS.md`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: Keep baseline performance evidence in machine-readable JSON artifacts and gate completeness with a non-zero-exit harness command.
  - Gotchas encountered: Harness intentionally fails until all metadata fields and all 21 scenario×mode rows are filled with metrics and `pass: true`.
  - Useful context: Browser verification for `US-003` remains manual here because browser MCP schemas/tools were unavailable in this environment.
---

## 2026-02-24 19:37:05 CST - US-003
- Hardened occlusion lifecycle cleanup by removing stale debug intersection graphics (`debug-*`) during occlusion teardown, preventing debug overlay leftovers after scene/canvas transitions.
- Files changed: `scripts/occlusion.js`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: Any debug stage overlays should have deterministic teardown hooks, especially for scene-switch and canvas teardown paths.
  - Gotchas encountered: This change adds one additional known lint warning (`_e` unused) in `scripts/occlusion.js` but does not introduce lint errors.
  - Useful context: `US-003` still requires manual profiling evidence and browser verification before marking `passes: true`.
---

## 2026-02-24 19:38:17 CST - US-003
- Replaced intentional no-op `catch (_e)` blocks in occlusion teardown/debug cleanup with optional catch binding (`catch {}`), preserving behavior while reducing non-actionable lint warnings in lifecycle cleanup paths.
- Files changed: `scripts/occlusion.js`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: Prefer optional catch binding in expected teardown races where the error object is intentionally unused.
  - Gotchas encountered: Lifecycle cleanup remains intentionally defensive because Foundry/PIXI may already have destroyed stage children during scene transitions.
  - Useful context: `US-003` still needs manual profiling artifact completion and manual browser verification before `passes` can be set to `true`.
---

## 2026-02-24 19:40:28 CST - US-004
- Hardened dynamic tile control behavior to eliminate no-controlled-token fallback rendering. The always-visible overlay now only uses actively controlled tokens, yielding deterministic empty layers when nothing is controlled and more predictable rapid control switching behavior.
- Files changed: `scripts/dynamictile.js`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: Prefer explicit controlled-token sources over observer/character fallbacks for overlay systems that must be deterministic.
  - Gotchas encountered: This change increases existing lint warning count because hook callback params are still unused in legacy handlers.
  - Useful context: Browser verification for rapid control switching and linked-door interactions is still manual in this environment.
---

## 2026-02-24 19:41:43 CST - US-004
- Hardened linked-wall update handling so dynamic tile overlays refresh when relevant linked walls are updated (`ds`, `door`, geometry/coords, flags) or deleted, preventing stale visibility state after wall/door lifecycle changes.
- Files changed: `scripts/dynamictile.js`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: Match linked-wall relationships by stored wall id values from tile flags to support deleted-wall refresh paths where wall objects no longer resolve from canvas.
  - Gotchas encountered: Expanding wall hooks increases existing warning count due unchanged legacy unused hook parameters, but introduces no lint errors.
  - Useful context: Browser verification for linked-door visibility transitions remains manual in this environment.
---

## 2026-02-24 - US-005
- Productionized token auto-sorting: added per-token coalescing in autosorting.js to eliminate sort jitter during rapid movement; documented sorting ownership (display z-index path in token.js vs document sort-write path in autosorting.js) in README; controlled-token visibility boost (+0.1 zIndex) already preserved in token.js.
- Files changed: `scripts/autosorting.js`, `README.md`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: Per-token update coalescing (running + needsRerun Sets) prevents conflicting document writes when updateToken fires rapidly during drag; only one update in flight per token, with re-run of latest state if another update arrives.
  - Gotchas encountered: autosorting.js is the single authoritative path for document.sort writes; token.js _refreshSort patch is display-only and must not write to document.
  - Useful context: US-005 has no browser verification requirement in acceptance criteria; smoke tests in SMOKE-TEST.md cover sort pipeline (6a) and rapid movement (5).
---

## 2026-02-24 - US-006
- Cleaned v13 runtime boundaries: removed ~370 lines of ambiguous commented experimental occlusion code from main.js (old occlusion layer, shaders, IsoOutlineFilter); removed unused imports (transform, consts, utils); fixed welcome.js irregular whitespace; fixed autosorting.js optional catch bindings; documented v11 legacy branches in Codebase Patterns; added Lint Baseline section to RELEASE-PROCESS.md. Lint warnings reduced from 82 to 66.
- Files changed: `scripts/main.js`, `scripts/welcome.js`, `scripts/autosorting.js`, `RELEASE-PROCESS.md`, `progress.txt`, `prd.json`.
- **Learnings for future iterations:**
  - Patterns discovered: main.js is the sole entry point (module.json esmodules); production occlusion lives in occlusion.js; legacy experiments are in archive/legacy-occlusion-experiments/.
  - Gotchas encountered: Removing a multi-line comment block in chunks can leave orphaned code; use smaller targeted replacements or verify file state after each edit.
  - Useful context: v11 branches (FOUNDRY_VERSION === 11) are already marked and not executed on v13; module compatibility is minimum 13.
---
